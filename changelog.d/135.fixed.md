- **The TUI's complexity signal breakdown now renders for real runs.** `core.prompt._complexity_meta` persists `meta.complexity.signals` as a mapping keyed by signal name (so a CEL condition can address `signals.prompt_size.contribution` by path), but `tui.complexity.read` only accepted the list shape `ComplexityScore.to_dict()` produces for `cof score`. The two shapes are both legitimate — one for CEL path addressing, one for `cof score`'s wire format — so the reader now accepts either; the inspector's signal-breakdown pane was showing "no signal breakdown recorded" for every real run before this fix.
