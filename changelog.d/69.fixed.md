- A missing, malformed, or non-object `--config` file now prints a single actionable `Error: …` line on stderr and exits 1 instead of dumping a traceback. Applies to every `-c/--config` command (`run`, `doctor`, `check`, `validate`, `list`, `info`, `eject`, `run-library`, `fetch`, `library refresh`, `gen`).
