- **Repo cleanup.** Removed the internal `_bmad/`/`_bmad-output/`/`.codex/` BMad workspace and its `.claude/commands/bmad-*.md` prompts, scratch top-level files (`input.json`, `my_orchestration.yml`, `orchestration-ideas.txt`, `config.example.json`, `requirements.txt`, `requirements.lock.txt`), the pre-`cof` `scripts/` wrappers, the duplicated top-level `rules/` tree (the runtime always read `src/circuitry/bundled/rules/`), and stale docs (Notion exports, BMad scan outputs, `docs/development-guide.md`, `docs/perceptron-boundary.md`, and the now-landed `docs/plans/`). No behavior changes.
