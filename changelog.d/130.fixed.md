- **`mypy src` is now clean under `pip install -e ".[tools]"`.** Fixed 8 pre-existing type errors in `plugins/python_eval.py` and `plugins/html_extract.py` that only surfaced with the optional `RestrictedPython`/`beautifulsoup4` deps installed — they were invisible to CI, which installed the package without `[tools]`. The `sandbox`/`mode` and HTML-node/attribute paths now carry real types instead of the implicit `Any` those extras had been masking.
