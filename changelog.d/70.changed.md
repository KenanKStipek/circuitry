- **Anthropic adapter defaults refreshed.** `default_model` is now
  `claude-sonnet-5` (was the stale `claude-sonnet-4-20250514`), and backend
  detection reports the current model aliases. `OllamaAdapter.list_models()`
  now returns tag names (`list[str]`) over stdlib `urllib` and degrades to
  an empty list when the daemon is unreachable, instead of returning the raw
  `/api/tags` payload and raising.
