- **Reflector planning language is now constrained to ASD-STE100 Simplified Technical English.** `REFLECTOR_PRIME_V1` gains a `=== LANGUAGE: SIMPLIFIED TECHNICAL ENGLISH ===` section teaching short, imperative, unambiguous sentences (one instruction per sentence, active voice, a 20/25-word procedural/descriptive ceiling, no vague verbs, no noun clusters over 3 words, articles never dropped) — applied to the plan's own text and to every generated effect's `template`. The worked example inside the prime is rewritten to imitate this register. A custom `prime_template` opts out of the constraint; `rules/reflector.yml` and `docs/orchestration-reference.md` document the override.
