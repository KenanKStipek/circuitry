- **`cof info` shows the resolved complexity settings and where each value came from.** The three switches (scoring/routing/decomposition), the routing band table (ordered, catch-all marked), the decomposition threshold, `max_depth`, and `on_failure` now render with their winning layer — config / orchestration / cli / default — extending the same `sources` provenance `resolve_effective_settings` already tracks for model/adapter (#151). `cof info` gains `--profile`, `--scoring/--no-scoring`, `--routing/--no-routing`, and `--decompose/--no-decompose` to preview how a run would resolve without actually running it, and `--json` carries the same `{value, source}` pairs for scripting.
