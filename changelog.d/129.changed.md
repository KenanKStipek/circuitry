- **Tooling.** `requirements-dev.txt` now pins `ruff<0.17` in addition to its
  existing floor, so a local `ruff check .` and CI's always see the same rule
  set. `CLAUDE.md`'s verification recipe now says explicitly to install from
  `requirements-dev.txt` rather than trust a `ruff`/`mypy` already on `PATH`.
