- **`docs/routing.md`.** The definitive reference for the complexity router: every scoring signal and its default weight (including the built-in `keywords` phrase table that applies even when `scoring.keywords` is left unset), the band table format with its ordering rules, catch-all requirement, and inclusive-`max` boundary condition, the full model precedence ladder and where the router sits in it, the three switches and their scoring prerequisite, and a worked example reproducing `docs/examples/routing/triage.yml` under both bundled band tables. It states plainly what the scorer cannot see — a static `cof score` preview measures the template, not the rendered prompt, and can score *lower* than a `--dry-run` render when a downstream reference to a not-yet-executed effect drops a keyword match — and cross-links `docs/complexity-config.md`, `docs/score-command.md`, and `docs/profiles.md` rather than duplicating them.
