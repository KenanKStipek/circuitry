- **Lint.** Fixed the 14 `ruff` findings on `main` left behind by #84 and
  #122 — implicit string concatenation inside `GENERIC_TEMPLATES` in
  `services/idea-farm/farm.py` is now parenthesized, and the two
  deliberately-formatted word blocks (`STOPWORDS`, `GENERIC_HOME_WORDS`) and
  the two `"\n".join([...])` test fixtures in `tests/service/test_idea_farm.py`
  keep their readable, multi-line form instead of collapsing to one long line.
