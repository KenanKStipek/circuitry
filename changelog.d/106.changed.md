- **`on_effect_complete` now fires on the failure path.** Prompt, tool, loop
  and dynamic effects previously re-raised without firing the hook when
  `on_error: fail`, which would have left the new `on_effect_start` unclosed.
  They now fire it — the node carrying `meta.error` — before re-raising, so
  start/complete pairs stay balanced when an effect fails.
