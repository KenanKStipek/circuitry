- Completed the `rules/` authoring ruleset: new `rules/use.yml` (the composition primitive) and `rules/interface.yml` (top-level typed I/O contract), plus documentation for every schema field previously missing from the rules — `retries`, `provider`, `provider_fallbacks`, `assets`, `inputs` (prompt), `min_iterations` (loop), `stop_on_error` (dynamic), `prime_template` (reflector), and `labels` / `description` / top-level `version` / `interface` (common). A new `tests/test_rules_coverage.py` guard asserts every property in `orchestration.schema.json` is mentioned in the assembled ruleset, that each rule file's example validates as an orchestration, and that `src/circuitry/bundled/rules/` stays in sync with `rules/`, so schema growth can no longer silently outpace the authoring guide.
