# CLI
typer[all]>=0.12
rich>=13.7

# Orchestration formats
pyyaml>=6.0
jsonschema>=4.0

# Template rendering (you’re already aligned here)
chevron>=0.14

# Optional but recommended (nice errors, speed)
python-dotenv>=1.0

ollama>=0.1.8

# CEL — `mode: cel` conditionals and loop `while` expressions
cel-python>=0.3
